name: Test MacOS Runner

on:
  workflow_dispatch:
    branches: [master]
permissions:
  contents: read  

jobs:
  test:
    runs-on: macos-latest
    name: CI Build
    steps:
    - name: Test Action
      uses: irongut/CodeCoverageSummary@f13848964841ca2f7a63343b287493e17dc645de # master
      with:
        filename: '/app/sample.coverage.xml'
        badge: 'true'
